# 計畫 A：Repo 與程式碼改善計畫

> 互 CARES 專題 · 工程面
> 適用期程：距總審約 5 個月
> 前提：**不另開新 repo**，於現有 fork（`dioispen/cares-mesh-app-android`）上重整

---

## 0. 目標與論點

**本計畫最佳化的對象是總審評分**；工程品質是手段，不是目的。這決定了排序：R4（實驗數據）是脊椎，其餘工程項目的價值取決於它們是否餵給 R4。

**論點**：Severity-aware relay 能讓緊急 Health Report 在壅塞的 mesh 中送達，且對一般流量的代價可接受。

**量測工具**：JVM 模擬（切點在 `PacketRelayManagerDelegate`），鏈路參數由 7 台實機校準。詳見 [ADR-0001](docs/adr/0001-policy-level-simulation-as-measurement-instrument.md)。

領域語彙見 [CONTEXT.md](CONTEXT.md)。**Status**（自我宣告的傷勢）與 **Severity**（衍生的繞送提示）是兩個不同的詞，不得混用「嚴重度」指涉兩者。

---

## 1. 問題清單

| # | 問題 | 嚴重度 | 對策 | 時程 |
|---|---|---|---|---|
| **R12** | **無模擬平台** — R4 的所有實驗都被它擋住，原計畫完全沒有此項 | **高** | 於 `PacketRelayManagerDelegate` 切點建模擬器 | **M1** |
| **R13** | **`ttl >= 4` 短路** — TTL 起始為 7，前三跳無條件轉傳，任何 severity 機制在多數送達路徑上都不生效 | **高** | 參數化該門檻 | **M1（R12 前置）** |
| R1 | main 分支為未修改的上游程式碼，本組零 commit | 高 | 合併工作分支進 main、重寫 README | 第 1 週 |
| R2 | GPLv3 義務未處理，fork 無來源說明 | 高 | README + NOTICE 標示 | 第 1 週 |
| R3 | 健康回報明文廣播姓名／電話／血型／精確座標 | 高 | **A1** Broadcast Tier（M2，實驗 5 的自變數）／**A2** Detail Tier（條件性，落後時第一個砍） | M2 |
| R4 | 無任何量測數據 | 高 | 六組核心實驗 + 三項延伸（見 §2） | M1–M4 |
| R5 | 嚴重度傳播策略 | 中高 | **2×2 因子設計**：F1 severity 加權轉傳機率 × F2 severity 優先佇列 | M2–M3 |
| R6 | 三端各自實作封包格式，無單一真相來源 | 中高 | **一致性測試向量**為權威，spec 文件為其敘述層；補 payload 版本位元組 | M2 |
| R7 | Firebase API key 公開且無 security rules | 中高 | 補 `firestore.rules` | M2 |
| R8 | 成員產出分布不均 | 中 | **每位成員擁有一個可獨立辯護的產出區域，且產出以檔案進入 repo**（見 §3） | 全程 |
| R9 | 分支混亂、commit 規範不一 | 中 | 清理分支 + branch protection | 第 1 週 |
| R10 | 測試幾近於無（Flutter 端僅有樣板 stub） | 中 | 由 R12 模擬器與 R6 測試向量構成主體 | M1–M2 |
| R11 | 無 release、無 APK、無 CI | 中 | 發布 release APK | M5 |

### 相對原始計畫的變更

- **新增 R12、R13**。原計畫缺少模擬平台，而它擋住整個 R4。
- **R5 重新定義**。原為「TTL／機率加權轉傳」；現為 F1×F2 因子設計。F1 單獨施行會**增加**總負載，在高負載下可能為負效果——只做 F1 而無 F2 對照，等於在第 4 個月才發現機制無效且無從比較。F2 需要新基礎設施（`mesh/` 目前沒有送出佇列），且**必須位於 `PacketRelayManagerDelegate` 邊界或其上層**，否則模擬器無法驅動。
- **R3 拆為 A1／A2**，A2 明確標為可砍。
- **R6 的強制手段改變**。spec 文件無人強制、寫完即開始腐爛；改以測試向量為權威，兩端測試讀同一份檔案，格式漂移會讓 `./gradlew test` 或 `flutter test` 失敗。
- **R8 改寫**。commit 數是審查者掃一眼的指標，實際被追問的是「這塊你能不能講清楚」。改為依可辯護的產出區域分工。
- **順序反轉**。實驗 1 是 kill-switch，移到 M1 末。

### 現況勘查

- `BitchatPacket.severity` 是**死欄位** — `BinaryProtocol.kt:79` 有編解碼，`app/src/main/java` 內 `protocol/` 以外零引用。線路格式上是一個免費的槽位。
- `K-of-N` 分支**並非 K-of-N**，只有一個 commit「新增緊急訊息功能，支持全連線廣播」，即 flooding，與 K-of-N 相反。
- 現行 relay 已是自適應機率轉傳（`networkSize <= 10` 時機率恆為 1.0），R5 是修改既有機制而非發明新機制。
- `mesh/` 內無送出佇列，`BluetoothPacketBroadcaster` 直接送出。
- Health Report payload **無版本位元組**。

---

## 2. 實驗矩陣（R4）

**因子**：F1 severity 加權轉傳機率（off/on）· F2 severity 優先佇列（off/on）· F3 payload 大小（現行 ~150 B vs Broadcast Tier ~18 B）· F4 Severity Inflation 比例（0/25/50/100 %）。掃描負載與節點數。
**指標**：**依 severity 分層**的送達率與端到端延遲，加上總傳輸次數作為成本項。

| # | 核心實驗 | 回答什麼 |
|---|---|---|
| 1 | **基線特性** — 未修改 relay，送達率／延遲 vs N 與負載 | 模型中究竟存不存在壅塞區間。**這一項可以否證整個論點，故最先跑。** |
| 2 | F1 單獨 vs 基線 | 加權轉傳機率有幫助，還是因增加負載而反效果 |
| 3 | F2 單獨 vs 基線 | 重新分配固定容量有沒有用 |
| 4 | F1+F2（完成 2×2） | 組合是否優於各自單獨，或互相干擾 |
| 5 | F3 於最佳組態 | 有多少效益其實只是「送得比較少」——A1 單獨是否已足夠 |
| 6 | F4 掃描於最佳組態 | Severity Inflation 讓機制退化回 flooding 的速度 |

| # | 延伸實驗 | 回答什麼 |
|---|---|---|
| 7 | 拓撲敏感度（均勻／叢集／線狀） | 結果在真實災區佈局下是否成立 |
| 8 | 節點流失與移動 | mesh 非靜態時是否成立 |
| 9 | **實機校準驗證** — 7 台手機量測值 vs 模擬 | 回答「這只是模擬」 |

**實驗 1 是 kill-switch。** 若校準後的容量模型在合理節點數與回報率下不產生壅塞，severity 加權就沒有可改善的對象，這件事必須在 M2 知道，不是 M5。屆時的退路是 A1（真實的隱私成果）、R6（一致性）與可行性研究。

排程注意：**九組實驗的成本在分析與撰寫，不在執行。** 模擬跑一輪近乎免費，多加一個 cell 不用錢，多加一個章節要。日程應圍繞撰寫來抓。

---

## 3. 分工（R8）

程式四人 · 非程式三人。**每人撰寫自己所負責區域的章節**——辯護你寫的，寫你負責的。

| 角色 | 產出 | 進 repo 的形式 |
|---|---|---|
| 程式 ×4 | R12 模擬器 · R13 · R5（F1/F2）· R3-A1 · R6 測試向量 · R7 | 程式碼與測試 |
| 實機校準 | 7 台手機測試場次、每跳延遲／單鏈路送達率／最大同時連線數 → 校準表 | 校準資料檔 + 實驗 9 章節 |
| 實驗分析 | 九組實驗的圖表與詮釋 | 分析腳本 + 結果章節 |
| 報告與相關研究 | 報告結構、related work、海報 | 報告原稿 |

實機校準有**長前置期**（借手機、找場地、重複跑），第 1 週就要開始規劃。

---

## 4. 時程

| 時間 | 關鍵路徑（4 人） | 平行工作（3 人） |
|---|---|---|
| **第 1 週** | R1 合併進 main · R2 NOTICE/GPLv3 · R9 分支清理 | 校準場次規劃：借手機、找場地 |
| **M1** | R13 參數化 `ttl>=4` · **R12 建模擬器** · **跑實驗 1** | 首批校準場次 |
| **M2** | F1 · F2 · R3-A1 Broadcast Tier · R6 測試向量 · R7 `firestore.rules` | 校準表完成 → 餵入模擬器 · related work |
| **M3** | 實驗 2–4（2×2）· 實驗 5（payload 大小） | 結果進來即開始分析製圖 |
| **M4** | 實驗 6–8 · **R3-A2 僅在超前時才做** | 實驗 9 · 各自章節 |
| **M5** | R11 release APK · CI | 報告整合 · 海報 |

**R7 置於 M2 的前提**：Firestore 內僅有自行測試的假資料，無任何真實使用者的健康資訊。若此前提改變，R7 立即上移至第 1 週——那不是總審問題。

---

## 5. 已記錄的決策

- [CONTEXT.md](CONTEXT.md) — 領域語彙
- [ADR-0001](docs/adr/0001-policy-level-simulation-as-measurement-instrument.md) — 以政策層模擬作為量測工具
- [ADR-0002](docs/adr/0002-severity-is-a-derived-transport-hint.md) — Severity 是衍生的傳輸提示
- [ADR-0003](docs/adr/0003-tiered-disclosure-for-health-reports.md) — Health Report 分層揭露
