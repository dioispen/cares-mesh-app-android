# Canonical repository-root local.properties for the build container.
#
# The embedded Flutter module reads a second, separate file:
# flutter_ui/.android/local.properties, which also needs flutter.sdk. That one
# is `flutter pub get` output and is written by prepare-flutter-module.sh.
sdk.dir=/opt/android-sdk
